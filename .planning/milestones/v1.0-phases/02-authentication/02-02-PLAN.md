---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/page.tsx
  - src/components/layout/nav-bar.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "User can visit /signup, fill in email and password, submit, and be redirected to the home page as a logged-in user"
    - "User can visit /login, fill in email and password, submit, and be redirected to the home page"
    - "When registration is locked (a user already exists), /signup shows 'Registration closed' with link to login"
    - "Unauthenticated users visiting / or any app route are redirected to /login"
    - "Auth pages (/login, /signup) have no nav bar -- centered card with 'My Anime Tracker' title above"
    - "App pages (/) show nav bar with avatar dropdown containing logout button"
    - "Clicking logout clears session and redirects to /login"
    - "Session persists across browser refresh and tab close/reopen"
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "Auth layout with centered card, no nav bar"
      min_lines: 10
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login form with email/password fields and inline errors"
      min_lines: 30
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Signup form with registration lock check, email/password fields, inline errors"
      min_lines: 30
    - path: "src/app/(app)/layout.tsx"
      provides: "Protected app layout with nav bar and session provider"
      min_lines: 15
    - path: "src/app/(app)/page.tsx"
      provides: "Home page (moved from root page.tsx)"
      min_lines: 5
    - path: "src/components/layout/nav-bar.tsx"
      provides: "Auth-aware nav bar with avatar dropdown and logout"
      min_lines: 30
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signIn.email()"
      pattern: "authClient\\.signIn"
    - from: "src/app/(auth)/signup/page.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signUp.email()"
      pattern: "authClient\\.signUp"
    - from: "src/app/(auth)/signup/page.tsx"
      to: "src/lib/auth.ts"
      via: "Server-side user count check for registration lock"
      pattern: "db\\.select.*users"
    - from: "src/components/layout/nav-bar.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.useSession() and authClient.signOut()"
      pattern: "authClient\\.(useSession|signOut)"
    - from: "src/app/(app)/layout.tsx"
      to: "src/lib/auth.ts"
      via: "Server-side session check with auth.api.getSession()"
      pattern: "auth\\.api\\.getSession"
---

<objective>
Build the authentication UI: login page, signup page, auth layout (centered card, no nav), protected app layout (with nav), auth-aware nav bar with logout dropdown, and route group restructuring.

Purpose: Complete the user-facing auth experience so users can sign up, log in, log out, and have sessions persist -- fulfilling all three AUTH requirements end-to-end.
Output: Working auth flow with login/signup pages, route protection, and session-aware nav bar.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-VALIDATION.md
@.planning/phases/02-authentication/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 outputs (will exist when this plan runs) -->

From src/lib/auth-client.ts:
```typescript
import { createAuthClient } from "better-auth/react"
export const authClient = createAuthClient()
// Client methods: authClient.signIn.email(), authClient.signUp.email(),
//   authClient.signOut(), authClient.useSession()
```

From src/lib/auth.ts:
```typescript
export const auth = betterAuth({ /* ... */ })
// Server-side: auth.api.getSession({ headers: await headers() })
```

From src/db/schema/users.ts (updated in Plan 01):
```typescript
export const users = pgTable('users', {
  id: uuid('id').defaultRandom().primaryKey(),
  email: text('email').notNull().unique(),
  emailVerified: boolean('email_verified').notNull().default(false),
  image: text('image'),
  name: text('name'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
})
```

From src/components/layout/nav-bar.tsx (WILL BE MODIFIED):
```typescript
// Current: static avatar with "U" fallback
// Target: avatar dropdown with logout, using authClient.useSession() for user state
```

From src/app/page.tsx (WILL BE MOVED to (app)/page.tsx):
```typescript
// Current: root page with NavBar + PlaceholderGrid
// Target: moved into (app) route group, NavBar moves to (app)/layout.tsx
```

From src/components/ui/ (already installed or will be in Plan 01):
```
card.tsx, input.tsx, label.tsx, button.tsx, dropdown-menu.tsx, avatar.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth pages and restructure into route groups</name>
  <files>
    src/app/(auth)/layout.tsx,
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/signup/page.tsx,
    src/app/(app)/layout.tsx,
    src/app/(app)/page.tsx,
    src/app/page.tsx,
    src/app/layout.tsx
  </files>
  <action>
    1. Create `src/app/(auth)/layout.tsx` -- Auth layout (no nav bar):
       - Full-height centered flexbox layout on warm dark background
       - Max-width container (max-w-md) centered with padding
       - "My Anime Tracker" as h1 above the form area (text-2xl, font-bold, centered, text-foreground, mb-6)
       - Children slot renders below the title
       - Strictly functional: no illustrations, decorative elements, or anime flair
       - Add explicit `ReactElement` return type per project ESLint rule

    2. Create `src/app/(auth)/login/page.tsx` -- Login page (client component):
       - Uses shadcn/ui Card, Input, Label, Button components
       - Form with email (type="email", required) and password (type="password", required, minLength=8) fields
       - Submit button labeled "Log in"
       - On submit: call `authClient.signIn.email({ email, password })` from `@/lib/auth-client`
       - On success: redirect to "/" using `window.location.href = "/"` (full page navigation to trigger proxy)
       - On error: show inline error message below the password field in red text (text-destructive, text-sm)
       - Error message wording: "Invalid email or password" (security-conscious -- never reveal whether email exists)
       - Link at bottom: "Don't have an account? Sign up" linking to /signup
       - Loading state: disable button and show "Logging in..." during submission
       - Add explicit `ReactElement` return type

    3. Create `src/app/(auth)/signup/page.tsx` -- Signup page (mixed server + client):
       - Server component wrapper checks if registration is open:
         - Query `db.select({ id: users.id }).from(users).limit(1)` from `@/db/drizzle` and `@/db/schema`
         - If user exists AND `process.env.ALLOW_REGISTRATION !== "true"`: render "Registration closed" Card with message and link to /login
         - If registration is open: render the SignupForm client component
       - SignupForm client component (can be in same file with "use client" boundary, or separate -- use judgment):
         - Same card layout as login: email + password fields
         - Submit button labeled "Create account"
         - On submit: call `authClient.signUp.email({ email, password, name: "" })`
         - On success: redirect to "/" (Better Auth autoSignIn is enabled, so session cookie is set)
         - On error: show inline error below relevant field. If error is "Registration is closed" (from databaseHook), show it below the submit button.
         - For generic errors: "Something went wrong. Please try again."
         - Link at bottom: "Already have an account? Log in" linking to /login
         - Loading state: disable button and show "Creating account..." during submission
       - Add explicit `ReactElement` return types

    4. Create `src/app/(app)/layout.tsx` -- Protected app layout:
       - Server component that checks session via `auth.api.getSession({ headers: await headers() })` from `@/lib/auth`
       - If no session: `redirect("/login")` from `next/navigation` (defense-in-depth, proxy is first line)
       - If session exists: render NavBar + children in the existing page structure
       - Pass session user email to NavBar if needed (or NavBar can use client-side useSession)

    5. Move current home page content:
       - Create `src/app/(app)/page.tsx` with the PlaceholderGrid content (NavBar is now in the (app) layout)
       - Import PlaceholderGrid from `@/components/shared/placeholder-grid`
       - Wrap in `<main className="mx-auto max-w-7xl">` as before

    6. Handle root `src/app/page.tsx`:
       - Replace the current root page with a redirect: server component that redirects to "/login" (or remove it entirely since proxy handles the redirect)
       - The simplest approach: delete the current content and make it redirect to "/" which the (app) route group catches. If Next.js route resolution prefers root page.tsx over (app)/page.tsx, then root page.tsx must be removed or become a redirect.
       - Safest: Remove `src/app/page.tsx` entirely. The (app)/page.tsx at `src/app/(app)/page.tsx` will handle "/" since route groups don't affect URL paths.

    7. Update `src/app/layout.tsx` -- Root layout stays mostly the same:
       - Keep ThemeProvider, Inter font, metadata
       - No changes needed since route groups handle layout differentiation

    Important per locked decisions:
    - Auth pages: centered minimal form card, warm dark background, strictly functional
    - Separate /login and /signup pages (distinct routes, linked to each other)
    - No nav bar on auth pages
    - Post-login redirect to home (placeholder grid)
    - Post-signup auto-login + same redirect
    - Inline error messages, red text, below relevant field
    - Error wording: "Invalid email or password" (don't reveal email existence)
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - Auth layout exists with centered card design, no nav bar, "My Anime Tracker" title
    - Login page with email/password form, inline errors, link to signup
    - Signup page with registration lock check, email/password form, inline errors, link to login
    - App layout with server-side session check and NavBar
    - Home page moved to (app) route group
    - Root page.tsx removed (route group handles /)
    - Build succeeds with no type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update NavBar with auth-aware avatar dropdown and logout</name>
  <files>
    src/components/layout/nav-bar.tsx
  </files>
  <action>
    1. Update `src/components/layout/nav-bar.tsx`:
       - Keep "use client" directive
       - Import `authClient` from `@/lib/auth-client`
       - Import DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger from `@/components/ui/dropdown-menu`
       - Use `authClient.useSession()` to get current session
       - Replace the static Avatar with a DropdownMenu:
         - Trigger: Avatar with user's email initial (first letter of email, uppercase) as AvatarFallback
         - Content: DropdownMenuContent aligned to end
         - Item 1: User's email displayed as disabled/label text (text-muted-foreground, text-sm)
         - Item 2: Separator
         - Item 3: "Log out" menu item
       - Logout handler: `await authClient.signOut()` then `window.location.href = "/login"` (full navigation to clear client state)
       - While session is loading (isPending): show the Avatar with "U" fallback (same as before, no flash)
       - Keep existing nav links, site title, and layout structure
       - Add explicit `ReactElement` return type

    Per locked decision: Logout button in avatar dropdown menu in nav bar (nav bar already has placeholder avatar).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - NavBar shows avatar with email initial when logged in
    - Clicking avatar opens dropdown with email and "Log out" option
    - Clicking "Log out" calls signOut and redirects to /login
    - NavBar builds without type errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete authentication flow</name>
  <files>n/a</files>
  <action>
    Human verifies the complete authentication flow end-to-end.

    <what-built>
      Complete authentication flow: signup, login, logout, session persistence, route protection, auth-aware nav bar.
    </what-built>
    <how-to-verify>
      Prerequisites: Docker PostgreSQL running (`make up`), dev server running (`pnpm dev`).

      1. **Route protection**: Visit http://localhost:3000/ -- should redirect to /login
      2. **Login page appearance**: Centered card on dark background, "My Anime Tracker" above, email + password fields, "Log in" button, no nav bar
      3. **Signup link**: Click "Don't have an account? Sign up" -- navigates to /signup
      4. **Signup page**: Same centered card layout with "Create account" button
      5. **Create account**: Enter an email and password (8+ chars), submit -- should redirect to home page with NavBar and PlaceholderGrid
      6. **NavBar**: Avatar in nav bar shows first letter of your email. Click it -- dropdown shows email and "Log out" option
      7. **Session persistence**: Refresh the page -- should stay logged in (not redirect to /login)
      8. **Session persistence**: Close the tab, open a new tab to http://localhost:3000/ -- should still be logged in
      9. **Logout**: Click avatar -> "Log out" -- should redirect to /login
      10. **Route protection after logout**: Try navigating to http://localhost:3000/ -- should redirect to /login
      11. **Registration lock**: Visit /signup again -- should show "Registration closed" message with link to login (because a user already exists)
      12. **Login with existing account**: Go to /login, enter the same credentials -- should log in and redirect to home
      13. **Error handling**: Try logging in with wrong password -- should show "Invalid email or password" below password field in red
    </how-to-verify>
    <resume-signal>Type "approved" to complete the phase, or describe any issues to fix</resume-signal>
  </action>
  <verify>Human visual and functional verification of 13-step auth flow checklist</verify>
  <done>All 13 verification steps pass: route protection, signup, login, session persistence, logout, registration lock, error handling</done>
</task>

</tasks>

<verification>
1. `pnpm test` -- all tests pass (existing + auth schema + registration lock + proxy)
2. `pnpm build` -- Next.js builds without errors
3. Full auth flow verified manually: signup -> auto-login -> refresh persists -> logout -> redirect to login -> re-login -> works
4. Registration lock: second signup attempt blocked
5. Route protection: unauthenticated access redirects to /login
</verification>

<success_criteria>
- User can create account at /signup with email and password
- User is auto-logged-in after signup and redirected to home
- User can log in at /login with email and password
- Session survives browser refresh and tab close/reopen
- Unauthenticated users redirected to /login for all app routes
- Auth pages have no nav bar, centered card with app title
- Nav bar has avatar dropdown with logout
- Registration locks after first user (shows "Registration closed")
- Inline error messages for auth failures (security-conscious wording)
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
