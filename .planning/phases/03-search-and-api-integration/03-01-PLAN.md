---
phase: 03-search-and-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/tracking/domain/entities/media-search-result.ts
  - src/modules/tracking/domain/ports/media-search-port.ts
  - src/modules/tracking/application/use-cases/search-media.ts
  - src/modules/tracking/application/use-cases/add-tracking-entry.ts
  - src/modules/tracking/infrastructure/adapters/anilist-adapter.ts
  - src/lib/anilist/rate-limiter.ts
  - next.config.ts
  - tests/search/anilist-adapter.test.ts
  - tests/search/search-result-mapping.test.ts
  - tests/search/rate-limiter.test.ts
  - tests/search/add-tracking-entry.test.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02

must_haves:
  truths:
    - "AniList GraphQL API can be queried for anime by title and results are mapped to domain entities"
    - "AniList GraphQL API can be queried for manga by title and results are mapped to domain entities"
    - "Unified search (no type filter) returns both anime and manga in a single call"
    - "Rate limiter prevents exceeding 25 req/min (safety margin under AniList 30 req/min degraded limit)"
    - "Add-to-list use case inserts a tracking entry with correct defaults and prevents duplicates"
    - "Null English titles fall back to Romaji titles in domain entity"
  artifacts:
    - path: "src/modules/tracking/domain/entities/media-search-result.ts"
      provides: "MediaSearchResult type with all SRCH-03 fields"
      exports: ["MediaSearchResult"]
    - path: "src/modules/tracking/domain/ports/media-search-port.ts"
      provides: "Port interface for search abstraction"
      exports: ["MediaSearchPort"]
    - path: "src/modules/tracking/application/use-cases/search-media.ts"
      provides: "Search use case calling port"
      exports: ["searchMedia"]
    - path: "src/modules/tracking/application/use-cases/add-tracking-entry.ts"
      provides: "Add-to-list use case with duplicate check"
      exports: ["addTrackingEntry"]
    - path: "src/modules/tracking/infrastructure/adapters/anilist-adapter.ts"
      provides: "AniList GraphQL fetch implementation"
      exports: ["AniListAdapter", "anilistAdapter"]
    - path: "src/lib/anilist/rate-limiter.ts"
      provides: "Token bucket rate limiter singleton"
      exports: ["anilistRateLimiter", "TokenBucket"]
  key_links:
    - from: "src/modules/tracking/application/use-cases/search-media.ts"
      to: "src/modules/tracking/infrastructure/adapters/anilist-adapter.ts"
      via: "MediaSearchPort interface"
      pattern: "MediaSearchPort"
    - from: "src/modules/tracking/infrastructure/adapters/anilist-adapter.ts"
      to: "src/lib/anilist/rate-limiter.ts"
      via: "anilistRateLimiter.tryConsume()"
      pattern: "anilistRateLimiter"
    - from: "src/modules/tracking/infrastructure/adapters/anilist-adapter.ts"
      to: "https://graphql.anilist.co"
      via: "fetch POST with GraphQL query"
      pattern: "fetch.*graphql\\.anilist\\.co"
---

<objective>
Build the AniList API adapter layer with hexagonal architecture: domain entities, port interface, AniList GraphQL adapter with rate limiting, search and add-to-list use cases, and comprehensive tests.

Purpose: Establishes the data access layer that the search UI (Plan 02) and all future tracking features depend on. Following hexagonal DDD, the domain defines the contract and the infrastructure implements it.

Output: Working, tested AniList adapter that can search anime/manga and an add-to-list use case that inserts tracking entries with duplicate prevention.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-and-api-integration/03-CONTEXT.md
@.planning/phases/03-search-and-api-integration/03-RESEARCH.md
@.planning/phases/03-search-and-api-integration/03-VALIDATION.md

@src/modules/tracking/domain/entities/.gitkeep
@src/modules/tracking/domain/ports/.gitkeep
@src/modules/tracking/application/use-cases/.gitkeep
@src/modules/tracking/infrastructure/adapters/.gitkeep
@src/db/schema/tracking.ts
@src/db/drizzle.ts
@src/lib/auth.ts
@next.config.ts

<interfaces>
<!-- Existing DB schema the adapter must align with -->

From src/db/schema/tracking.ts:
```typescript
export const trackingStatusEnum = pgEnum('tracking_status', [
  'watching', 'completed', 'on_hold', 'dropped', 'plan_to_watch',
])
export const mediaTypeEnum = pgEnum('media_type', ['anime', 'manga'])
export const trackingEntries = pgTable('tracking_entries', {
  id: uuid('id').defaultRandom().primaryKey(),
  userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
  anilistId: integer('anilist_id').notNull(),
  mediaType: mediaTypeEnum('media_type').notNull(),
  status: trackingStatusEnum('status').notNull().default('plan_to_watch'),
  progress: integer('progress').notNull().default(0),
  rating: integer('rating'),
  notes: text('notes'),
  titleEnglish: text('title_english'),
  titleRomaji: text('title_romaji'),
  coverImageUrl: text('cover_image_url'),
  totalEpisodes: integer('total_episodes'),
  totalChapters: integer('total_chapters'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
})
```

From src/db/schema/index.ts:
```typescript
export { users } from './users'
export { mediaTypeEnum, trackingEntries, trackingStatusEnum } from './tracking'
export { accounts, sessions, verifications } from './auth'
```

From src/db/drizzle.ts:
```typescript
export const db = createDb()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain contracts, AniList adapter, and rate limiter with tests</name>
  <files>
    src/modules/tracking/domain/entities/media-search-result.ts,
    src/modules/tracking/domain/ports/media-search-port.ts,
    src/modules/tracking/infrastructure/adapters/anilist-adapter.ts,
    src/lib/anilist/rate-limiter.ts,
    next.config.ts,
    tests/search/anilist-adapter.test.ts,
    tests/search/search-result-mapping.test.ts,
    tests/search/rate-limiter.test.ts
  </files>
  <behavior>
    - MediaSearchResult entity has: id (number), title (string, resolved from english ?? romaji), titleEnglish (string | null), titleRomaji (string), type ('ANIME' | 'MANGA'), status (AniList MediaStatus string), episodes (number | null), chapters (number | null), coverImageUrl (string | null), coverImageColor (string | null)
    - MediaSearchPort interface has: searchMedia(query: string, perPage?: number) => Promise<MediaSearchResult[]>
    - AniListAdapter implements MediaSearchPort, sends POST to https://graphql.anilist.co with the verified GraphQL query (from RESEARCH.md), sorts by POPULARITY_DESC, filters isAdult: false, default perPage=15
    - AniListAdapter checks anilistRateLimiter.tryConsume() before fetch; throws descriptive error if rate limited
    - AniListAdapter maps raw API response to MediaSearchResult[] correctly (including null title.english fallback)
    - AniListAdapter handles: 429 rate limit (throws with retry info), network errors (throws descriptive error), GraphQL errors in response body (throws with error message), empty results (returns [])
    - TokenBucket: starts at capacity 25, refill rate 25/60 tokens per second; tryConsume returns true and decrements when tokens >= 1; tryConsume returns false when empty; refills over time; never exceeds capacity
    - next.config.ts has remotePatterns for s4.anilist.co
  </behavior>
  <action>
    1. Create `src/modules/tracking/domain/entities/media-search-result.ts`:
       - Export `MediaSearchResult` interface with fields: id (number), title (string -- resolved display title), titleEnglish (string | null), titleRomaji (string), type ('ANIME' | 'MANGA'), status (string), episodes (number | null), chapters (number | null), coverImageUrl (string | null), coverImageColor (string | null)
       - Export `mapAniListMedia(raw: AniListMediaResponse): MediaSearchResult` function that handles null english title fallback (english ?? romaji)
       - Export the `AniListMediaResponse` raw type matching the verified API response shape from RESEARCH.md

    2. Create `src/modules/tracking/domain/ports/media-search-port.ts`:
       - Export `MediaSearchPort` interface with `searchMedia(query: string, perPage?: number): Promise<MediaSearchResult[]>`

    3. Create `src/lib/anilist/rate-limiter.ts`:
       - Export `TokenBucket` class with constructor(capacity, refillRate), tryConsume() method
       - Export `anilistRateLimiter` singleton: capacity 25, refill rate 25/60 (stays safely under AniList's degraded 30 req/min limit)

    4. Create `src/modules/tracking/infrastructure/adapters/anilist-adapter.ts`:
       - Export `AniListAdapter` class implementing `MediaSearchPort`
       - Use the exact GraphQL query from RESEARCH.md (verified live): `query SearchMedia($search: String!, $page: Int, $perPage: Int) { Page(page: $page, perPage: $perPage) { media(search: $search, sort: POPULARITY_DESC, isAdult: false) { id title { english romaji } type status episodes chapters coverImage { large color } } } }`
       - Check `anilistRateLimiter.tryConsume()` before fetching; throw Error('Rate limit exceeded. Please wait before searching again.') if false
       - Use native fetch with `next: { revalidate: 300 }` for 5-minute cache
       - Handle HTTP 429 (extract Retry-After), non-200 status, GraphQL errors in body, network failures
       - Map response through `mapAniListMedia`
       - Export `anilistAdapter` singleton instance

    5. Update `next.config.ts`:
       - Add `images.remotePatterns` with `{ protocol: 'https', hostname: 's4.anilist.co', pathname: '/file/anilistcdn/**' }`

    6. Write tests (TDD -- write tests first, then implement):
       - `tests/search/rate-limiter.test.ts`: Test TokenBucket -- starts full, drains to zero, rejects when empty, refills over time (use vi.advanceTimersByTime), never exceeds capacity
       - `tests/search/search-result-mapping.test.ts`: Test mapAniListMedia -- maps all fields correctly, falls back to romaji when english is null, handles null coverImage, handles null episodes/chapters
       - `tests/search/anilist-adapter.test.ts`: Mock global fetch with vi.stubGlobal. Test: successful search returns mapped results, rate limit rejection throws before fetch, HTTP 429 throws with retry info, GraphQL errors in body throw, network failure throws, empty results return []

    Remove .gitkeep files from directories that now have real files.
  </action>
  <verify>
    <automated>cd /Users/o105/Sites/SpykeRel04D/manga-anime-tracker && pnpm vitest run tests/search/rate-limiter.test.ts tests/search/search-result-mapping.test.ts tests/search/anilist-adapter.test.ts</automated>
  </verify>
  <done>
    - MediaSearchResult entity exists with all SRCH-03 fields and null-safe title resolution
    - MediaSearchPort interface defines the search contract
    - AniListAdapter implements the port, queries AniList GraphQL, maps responses, handles all error cases
    - TokenBucket rate limiter enforces 25 req/min ceiling
    - next.config.ts allows AniList CDN images
    - All tests pass (rate limiter, response mapping, adapter with mocked fetch)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Search and add-to-list use cases with tests</name>
  <files>
    src/modules/tracking/application/use-cases/search-media.ts,
    src/modules/tracking/application/use-cases/add-tracking-entry.ts,
    tests/search/add-tracking-entry.test.ts
  </files>
  <behavior>
    - searchMedia(query) calls anilistAdapter.searchMedia(query) and returns results; returns [] for empty/whitespace query
    - addTrackingEntry(userId, data) inserts into trackingEntries table with mediaType-aware default status: 'plan_to_watch' for both anime and manga (the enum only has plan_to_watch, not plan_to_read)
    - addTrackingEntry checks for existing entry with same (userId, anilistId); returns { success: false, error: 'already_tracked' } if duplicate
    - addTrackingEntry returns { success: true } on successful insert
  </behavior>
  <action>
    1. Create `src/modules/tracking/application/use-cases/search-media.ts`:
       - Import `anilistAdapter` from infrastructure adapter
       - Export `async function searchMedia(query: string): Promise<MediaSearchResult[]>`
       - Trim and validate query -- return [] if empty
       - Delegate to `anilistAdapter.searchMedia(query)`

    2. Create `src/modules/tracking/application/use-cases/add-tracking-entry.ts`:
       - Import `db` from `@/db/drizzle`, `trackingEntries` from `@/db/schema`
       - Import `eq, and` from `drizzle-orm`
       - Export input type `AddTrackingEntryInput`: { anilistId: number, mediaType: 'anime' | 'manga', titleEnglish: string | null, titleRomaji: string | null, coverImageUrl: string | null, totalEpisodes: number | null, totalChapters: number | null }
       - Export result type `AddTrackingEntryResult`: { success: true } | { success: false, error: 'already_tracked' | 'not_authenticated' }
       - Export `async function addTrackingEntry(userId: string, data: AddTrackingEntryInput): Promise<AddTrackingEntryResult>`
       - Check for existing entry: `SELECT id FROM tracking_entries WHERE user_id = userId AND anilist_id = data.anilistId`
       - If exists, return `{ success: false, error: 'already_tracked' }`
       - Otherwise insert with status 'plan_to_watch' (default for all media types per schema enum)
       - Return `{ success: true }`

    3. Write tests (TDD):
       - `tests/search/add-tracking-entry.test.ts`: Mock db module. Test: successful insert returns { success: true }, duplicate returns { success: false, error: 'already_tracked' }, correct fields passed to insert

    Remove .gitkeep from use-cases directory if it exists.
  </action>
  <verify>
    <automated>cd /Users/o105/Sites/SpykeRel04D/manga-anime-tracker && pnpm vitest run tests/search/add-tracking-entry.test.ts</automated>
  </verify>
  <done>
    - searchMedia use case wraps adapter with input validation
    - addTrackingEntry use case handles insert with duplicate prevention
    - Test confirms duplicate detection and successful insert behavior
  </done>
</task>

</tasks>

<verification>
All domain and infrastructure code is tested:
```bash
cd /Users/o105/Sites/SpykeRel04D/manga-anime-tracker && pnpm vitest run tests/search/
```
Expected: All tests pass (rate limiter, response mapping, adapter, add-tracking-entry).

Full test suite still green:
```bash
pnpm test
```
</verification>

<success_criteria>
- AniList adapter can search for anime and manga by title (SRCH-01, SRCH-02 backend support)
- Response mapping correctly extracts all fields required by SRCH-03
- Rate limiter prevents exceeding AniList's degraded 30 req/min limit
- Add-to-list use case inserts entries and prevents duplicates
- All tests pass with mocked dependencies (no live API calls in tests)
- next.config.ts allows AniList CDN images
</success_criteria>

<output>
After completion, create `.planning/phases/03-search-and-api-integration/03-01-SUMMARY.md`
</output>
